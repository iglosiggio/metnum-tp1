#include "colleyMatrixMethod.h"

using namespace std;

vector<double> colleyMatrixMethod::calculateLeaderboard(const int teamQuantity, const int matchesQuantity, const vector<vector<int>> resultMatrix) {
    vector<double> results(teamQuantity, 0.0);

    //TODO: hacer el algoritmo para este metodo de calculacion de rankings.

    return results;
}
